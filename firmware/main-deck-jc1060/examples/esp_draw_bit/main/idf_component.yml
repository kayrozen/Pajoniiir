dependencies:
  espressif/usb: "^1.0.0"
  espressif/esp_hosted:
    version: "^2.0.0"
    rules:
    - if: target in [esp32p4]
  espressif/esp_wifi_remote:
    version: ">=1.3.1"
    rules:
    - if: target in [esp32p4]
